name: bloc_architecture
description: Example BLoC architecture

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.0
  rxdart: ^0.24.1
  http: ^0.12.2
  json_annotation: ^3.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_runner: ^1.10.1
  json_serializable: ^3.5.0

flutter:
  uses-material-design: true

  assets:
    - assets/
